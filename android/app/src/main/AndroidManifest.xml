<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Network ──────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ── Location (for GPS prayer times) ─────────────────────────────── -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- ── Notifications ────────────────────────────────────────────────── -->
    <!-- POST_NOTIFICATIONS: required on Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- SCHEDULE_EXACT_ALARM: required on Android 12+ (API 31+) for
         flutter_local_notifications zonedSchedule exactAllowWhileIdle. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- USE_EXACT_ALARM: auto-granted for alarm/prayer/calendar apps (API 33+).
         Ensures exact alarms work without user needing to grant manually. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- RECEIVE_BOOT_COMPLETED: reschedule notifications after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- FOREGROUND_SERVICE: required on Android 9+ (API 28+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- WAKE_LOCK: keep CPU awake while processing exact alarms -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="Diyaa"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding version -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- ── flutter_local_notifications receivers ──────────────────── -->

        <!-- Fires scheduled notifications.
             directBootAware: alarms fire immediately after reboot even
             before the user unlocks (Direct Boot mode, API 24+). -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"
            android:directBootAware="true"/>

        <!-- Reschedules all notifications after device reboot or app update. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- NOTE: The <service> entry for FlutterLocalNotificationsPlugin
             has been intentionally removed. That class is NOT a Service
             subclass — declaring it as one causes an [Instantiatable] lint
             error that blocks release builds. The plugin works correctly
             without this entry; it registers its own service internally
             via its AAR manifest merger. -->

        <!-- ── Salah sound receiver ──────────────────────────────────────── -->
        <!-- BroadcastReceiver that plays the Salah 'ala Al-Nabi sound via
             MediaPlayer when a native AlarmManager alarm fires. This is a
             fallback for the unreliable notification channel sound mechanism.
             The alarm is scheduled via MethodChannel from Dart and fires even
             in Doze mode (setExactAndAllowWhileIdle). -->
        <receiver android:name=".SalahSoundReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

    <!-- Required for Flutter text processing plugin -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
